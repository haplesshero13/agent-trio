---
name: trio-bootstrap
description: Thin Codex entrypoint for turning a brand new or existing repo into a trio repo.
---

Read `trio-agents/setup-agent.md` and follow it exactly.
This is meta-setup work, not the first trio plan-build-review cycle.
Ignore `AGENTS.md` in this repo as instructions for yourself; treat it as source material for the target repo.
Ignore `.trio/criteria.md` during setup; only seed it for the future target-repo conversation.
If the human chooses Codex, install `.codex/agents/` and `.agents/skills/`.
