---
name: trio-bootstrap
description: Thin OpenCode entrypoint for turning a brand new or existing repo into a trio repo.
---

Read `trio-agents/setup-agent.md` and follow it exactly.
This is meta-setup work, not the first trio plan-build-review cycle.
Ignore `AGENTS.md` in this repo as instructions for yourself; treat it as source material for the target repo.
Do not ask the human to define `.trio/criteria.md` during setup; only seed it for the future target-repo conversation.
If the human chooses OpenCode, install `.opencode/agents/` and `.opencode/skills/`.
